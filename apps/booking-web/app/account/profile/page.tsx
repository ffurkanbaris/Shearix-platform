"use client";import Link from "next/link";import {FormEvent,useEffect,useState} from "react";import {useRouter} from "next/navigation";import {ApiError,apiClient} from "@/lib/api";export default function Profile(){const router=useRouter();const[name,setName]=useState("");const[email,setEmail]=useState("");const[saved,setSaved]=useState(false);const[error,setError]=useState("");const[busy,setBusy]=useState(false);useEffect(()=>{void apiClient.get<{name:string;email:string}>("/v1/public/customer/profile").then(x=>{setName(x.name);setEmail(x.email)}).catch(e=>{if(e instanceof ApiError&&e.status===401)router.replace("/login");else setError("We could not load your profile. Try again.")})},[router]);async function save(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{await apiClient.patch("/v1/public/customer/profile",{name});setSaved(true)}catch(e){setError(e instanceof ApiError?e.message:"Unable to save profile.")}finally{setBusy(false)}}return <main className="account-page"><Link className="eyebrow" href="/account">← Account</Link><h1>Profile</h1><form className="account-form" onSubmit={save}><label>Name<input value={name} onChange={e=>setName(e.target.value)} required /></label><label>Email<input value={email} disabled /></label>{error&&<p className="error" role="alert">{error}</p>}<button className="book-button" disabled={busy}>{busy?"Saving…":"Save profile"}</button>{saved&&<p className="success-note">Profile saved.</p>}</form></main>}
